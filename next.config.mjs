/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['victornepo.somosdevteam.com'], // Domínio adicionado corretamente
    },
};

export default nextConfig;
